---
 - hosts: all
   gather_facts: no
   tasks:
 - name: build container image
      docker_image:
        name: tomcat:latest
        build:
          path: /root/
          source: build
        state: present
